spring:
  profiles:
    active: test

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
      open-in-view: false

logging.level:
  org.hibernate.type: trace